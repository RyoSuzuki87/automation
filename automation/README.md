# Simple ToDo (automation)

Minimal Flutter ToDo app stored under `automation/`.  
It persists tasks to `SharedPreferences` as JSON string list.

## Run locally

\`\`\`bash
cd automation
flutter pub get
flutter run
